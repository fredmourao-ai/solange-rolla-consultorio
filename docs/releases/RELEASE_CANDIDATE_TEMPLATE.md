# Release Candidate

## Identificação

- Versão:
- SHA completo:
- Data/hora UTC:
- Ambiente: `staging` / `production`
- Responsável técnico:
- Responsável operacional:

## Gates

- CI: `PASS` / `FAIL`, URL:
- Database/pgTAP: `PASS` / `FAIL`, URL:
- E2E/security/accessibility: `PASS` / `FAIL`, URL:
- Architecture/module/migration checks: `PASS` / `FAIL`, URL:
- Dependency audit/security advisories: `PASS` / `FAIL`, evidência:
- Backup/restore e RPO/RTO: `PASS` / `FAIL`, evidência:
- Rollback: `PASS` / `FAIL`, evidência:

## Jurídico, fiscal e operação

- Textos versionados/revisados: `PASS` / `FAIL`.
- Formulário real homologado: `PASS` / `FAIL`.
- Emissor e tratamentos NFS-e aprovados: `PASS` / `FAIL`.
- Secrets/flags/domínio/MFA verificados: `PASS` / `FAIL`.
- Smoke sintético e ausência de seed: `PASS` / `FAIL`.

## Pendências e riscos

| Item | Tipo | Responsável | Bloqueia? | Plano/data |
| --- | --- | --- | --- | --- |
|  | Externa ou interna |  | Sim/Não |  |

## Decisão

**NO-GO** até que todas as condições críticas de `docs/operations/GO_LIVE_CHECKLIST.md` tenham evidência.

Decisão final: `GO` / `NO-GO`  
Aprovadores:  
Data:  
Observações:
