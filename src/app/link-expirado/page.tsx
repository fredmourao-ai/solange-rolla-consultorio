export default function ExpiredLinkPage() {
  return (
    <main className="auth-page">
      <section className="auth-panel" aria-labelledby="expired-link-title">
        <h1 id="expired-link-title">Link indisponível</h1>
        <p>Este link não está mais disponível. Solicite um novo link ao consultório.</p>
      </section>
    </main>
  )
}
