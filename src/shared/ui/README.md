# Shared UI

Shared primitives are semantic, accessible and free of business rules. Use
these components before creating a module-specific control. Business modules
may compose them but must not import another module's internals.

All controls preserve visible keyboard focus, use a 44px minimum touch target,
and expose status through text as well as color.
